apiVersion: v1
kind: Service
metadata:
  name: mehaat
  namespace: mehaat
  labels:
    app.kubernetes.io/name: mehaat
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mehaat
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
